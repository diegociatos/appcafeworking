# Sem desvio geral de propósito.
#
# O app roteia por query string (/?p=financeiro), então nenhuma rota precisa de
# caminho próprio. A regra antiga "/* /index.html 200" mandava também
# /assets/x.js inexistente para o index com status 200; como /assets/* leva
# Cache-Control immutable de um ano, o navegador guardava HTML no lugar do
# módulo durante a troca de deploy e o app abria em branco.
#
# Cloudflare Pages não aceita status 404 no _redirects, então a única forma de
# um asset ausente devolver 404 é não existir desvio que o capture.
